import { useState } from 'react';
import { ChevronRight, PackageCheck, X } from 'lucide-react';
const ORDERS = [{ id: 'JC-3021', garment: 'Barong Tagalog', fabric: 'Piña Jusi — Ivory', stage: 'First Fitting', due: 'Aug 12, 2026', balance: '₱2,400 due on release' }, { id: 'JC-3019', garment: "Women's Coat", fabric: 'Wool Blend — Camel', stage: 'Final Alterations', due: 'Aug 10, 2026', balance: 'Paid in full' }];
export function CustomerOrdersView() { const [selected, setSelected] = useState<typeof ORDERS[number] | null>(null); return <div className="space-y-6"><header className="border-b-4 border-[#D8A927] bg-[#0F1F3D] p-7 text-white"><Tag>Order studio</Tag><h1 className="mt-1 text-4xl font-bold" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>My Orders</h1><p className="mt-2 text-sm text-[#C7D6EB]">Follow each garment from the first measurement to final pickup.</p></header><div className="grid gap-4 lg:grid-cols-2">{ORDERS.map((order) => <button key={order.id} onClick={() => setSelected(order)} className="border border-dashed border-[#9DB1CB] bg-white p-6 text-left shadow-sm hover:border-[#0F1F3D]"><Tag>{order.id}</Tag><h2 className="mt-3 text-2xl font-bold text-[#0F1F3D]" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>{order.garment}</h2><p className="mt-1 text-sm text-[#4C6E93]">{order.fabric}</p><div className="mt-6 flex items-center justify-between border-t border-[#DCE5EF] pt-4"><div><Tag>Production stage</Tag><p className="mt-1 text-sm font-semibold text-[#0F1F3D]">{order.stage}</p></div><ChevronRight className="h-5 w-5 text-[#D8A927]" /></div></button>)}</div>{selected && <div className="fixed inset-0 z-50 flex items-center justify-center p-4"><button onClick={() => setSelected(null)} className="absolute inset-0 bg-[#0F1F3D]/70" /><section className="relative w-full max-w-lg border-t-4 border-[#D8A927] bg-white p-7 shadow-2xl"><button onClick={() => setSelected(null)} className="absolute right-5 top-5"><X className="h-5 w-5" /></button><Tag>Order details</Tag><h2 className="mt-2 text-3xl font-bold text-[#0F1F3D]" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>{selected.id}</h2><div className="mt-6 grid grid-cols-2 gap-3">{[['Garment', selected.garment], ['Fabric', selected.fabric], ['Current stage', selected.stage], ['Estimated ready', selected.due]].map(([label, value]) => <Detail key={label} label={label} value={value} />)}</div><p className="mt-5 flex items-center gap-2 text-sm font-medium text-[#0F1F3D]"><PackageCheck className="h-4 w-4 text-[#D8A927]" />{selected.balance}</p></section></div>}</div>; } function Tag({ children }: { children: React.ReactNode }) { return <span className="text-[10px] uppercase tracking-[0.2em] text-[#5A769A]" style={{ fontFamily: "'IBM Plex Mono', monospace" }}>{children}</span>; } function Detail({ label, value }: { label: string; value: string }) { return <div className="border border-[#DCE5EF] bg-[#F8FBFF] p-3"><Tag>{label}</Tag><p className="mt-1 text-sm font-medium text-[#0F1F3D]">{value}</p></div>; } export default CustomerOrdersView;
