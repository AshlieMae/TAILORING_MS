import { useState } from 'react';
import { Bell, Check, Save, UserRound } from 'lucide-react';
export function CustomerSettingsView() { const [settings, setSettings] = useState({ name: 'Reyna Fuentes', email: 'reyna.fuentes@email.com', reminders: true, updates: true }); const [saved, setSaved] = useState(false); const save = () => { setSaved(true); window.setTimeout(() => setSaved(false), 3000); }; return <div className="max-w-5xl space-y-6"><header className="border-b-4 border-[#D8A927] bg-white p-7"><span className="text-[10px] uppercase tracking-[0.2em] text-[#5A769A]">Account controls</span><h1 className="mt-1 text-4xl font-bold text-[#0F1F3D]" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>Settings</h1></header>{saved && <div className="flex items-center gap-2 border border-[#A8C9B5] bg-[#EDF7F0] p-3 text-sm text-[#39705B]"><Check className="h-4 w-4" />Preferences saved.</div>}<Section icon={<UserRound className="h-5 w-5" />} title="Profile"><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name" value={settings.name} onChange={(value) => setSettings({ ...settings, name: value })} /><Field label="Email address" value={settings.email} onChange={(value) => setSettings({ ...settings, email: value })} /></div></Section><Section icon={<Bell className="h-5 w-5" />} title="Notifications"><Toggle label="Fitting reminders" checked={settings.reminders} onClick={() => setSettings({ ...settings, reminders: !settings.reminders })} /><Toggle label="Order updates" checked={settings.updates} onClick={() => setSettings({ ...settings, updates: !settings.updates })} /></Section><button onClick={save} className="inline-flex items-center gap-2 bg-[#0F1F3D] px-5 py-3 text-[10px] font-semibold uppercase tracking-[0.14em] text-white"><Save className="h-4 w-4" />Save settings</button></div>; } function Section({ icon, title, children }: { icon: React.ReactNode; title: string; children: React.ReactNode }) { return <section className="border border-[#DCE5EF] bg-white p-6"><div className="flex items-center gap-3 text-[#0F1F3D]">{icon}<h2 className="text-xl font-bold">{title}</h2></div><div className="mt-6">{children}</div></section>; } function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="text-xs font-medium text-[#4C6E93]">{label}<input value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full border border-[#C9D8E8] px-3 py-2.5 text-sm text-[#0F1F3D] outline-none" /></label>; } function Toggle({ label, checked, onClick }: { label: string; checked: boolean; onClick: () => void }) { return <div className="flex items-center justify-between border-b border-[#DCE5EF] py-4 last:border-0"><span className="text-sm font-medium text-[#0F1F3D]">{label}</span><button onClick={onClick} className={`h-6 w-11 rounded-full p-1 ${checked ? 'bg-[#0F1F3D]' : 'bg-[#B7C5D4]'}`}><span className={`block h-4 w-4 rounded-full bg-white transition-transform ${checked ? 'translate-x-5' : ''}`} /></button></div>; } export default CustomerSettingsView;
