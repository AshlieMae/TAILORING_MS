import { useState } from 'react';
import { CalendarClock, Check } from 'lucide-react';
const INITIAL = [{ id: 'APT-106', date: 'Aug 12, 2026', time: '3:30 PM', type: 'First Fitting', job: 'JC-3021', status: 'Confirmed' }, { id: 'APT-112', date: 'Aug 16, 2026', time: '10:00 AM', type: 'Final Fitting', job: 'JC-3021', status: 'Scheduled' }];
export function CustomerAppointmentsView() { const [appointments, setAppointments] = useState(INITIAL); const confirm = (id: string) => setAppointments((current) => current.map((appointment) => appointment.id === id ? { ...appointment, status: 'Confirmed' } : appointment)); return <div className="space-y-6"><header className="flex flex-col gap-4 border border-[#C9D8E8] bg-[#EAF1F8] p-7 sm:flex-row sm:items-end sm:justify-between"><div><span className="text-[10px] uppercase tracking-[0.2em] text-[#5A769A]">Fitting calendar</span><h1 className="mt-1 text-4xl font-bold text-[#0F1F3D]" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>Appointments</h1><p className="mt-2 text-sm text-[#4C6E93]">Your upcoming tailoring appointments.</p></div><CalendarClock className="h-10 w-10 text-[#0F1F3D]" /></header><section className="divide-y divide-[#DCE5EF] border border-[#DCE5EF] bg-white">{appointments.map((appointment) => <article key={appointment.id} className="flex flex-col gap-4 p-6 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-5"><div className="border-r border-[#DCE5EF] pr-5 text-center"><span className="block text-xl font-bold text-[#0F1F3D]" style={{ fontFamily: "'Big Shoulders Display', sans-serif" }}>{appointment.date.split(' ')[1].replace(',', '')}</span><span className="text-[10px] uppercase tracking-[0.15em] text-[#5A769A]">Aug</span></div><div><span className="text-[10px] uppercase tracking-[0.16em] text-[#5A769A]">{appointment.job}</span><h2 className="mt-1 text-lg font-bold text-[#0F1F3D]">{appointment.type}</h2><p className="text-sm text-[#4C6E93]">{appointment.date} · {appointment.time}</p></div></div>{appointment.status === 'Confirmed' ? <span className="inline-flex items-center gap-1 text-xs font-semibold text-[#39705B]"><Check className="h-4 w-4" />Confirmed</span> : <button onClick={() => confirm(appointment.id)} className="bg-[#0F1F3D] px-4 py-2.5 text-[10px] font-semibold uppercase tracking-[0.14em] text-white">Confirm attendance</button>}</article>)}</section></div>; } export default CustomerAppointmentsView;
