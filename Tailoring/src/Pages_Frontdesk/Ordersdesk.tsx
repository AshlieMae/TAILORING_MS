import { useMemo, useState } from 'react';
import { ChevronRight, FilePlus2, Search, TrendingUp, X } from 'lucide-react';
import {
  ResponsiveContainer,
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Cell,
} from 'recharts';
import { CreateOrderModal, type NewOrderForm } from '../dashboard/Frontdeskdashboard';

type OrderStatus = 'Measuring' | 'Pattern Cutting' | 'First Fitting' | 'Final Alterations' | 'Ready for Pickup';
type Order = { id: string; customer: string; garment: string; fabric: string; due: string; status: OrderStatus; payment: string };
const INITIAL_ORDERS: Order[] = [{ id: 'JC-3021', customer: 'Reyna Fuentes', garment: 'Barong Tagalog', fabric: 'Piña Jusi — Ivory', due: 'Aug 05', status: 'First Fitting', payment: 'Deposit paid' }, { id: 'JC-3020', customer: 'Boyet Salcedo', garment: 'Two-piece Suit', fabric: 'Italian Wool — Charcoal', due: 'Aug 09', status: 'Pattern Cutting', payment: 'Deposit paid' }, { id: 'JC-3019', customer: 'Consuelo Reyes', garment: "Women's Coat", fabric: 'Wool Blend — Camel', due: 'Aug 03', status: 'Final Alterations', payment: 'Paid' }, { id: 'JC-3018', customer: 'Tomas Villareal', garment: 'School Uniform Set', fabric: 'Polyester — Navy', due: 'Aug 02', status: 'Ready for Pickup', payment: 'Balance due' }];
const stageStyle: Record<OrderStatus, string> = { Measuring: 'border-[#D9C8B7] bg-[#F8F3EB] text-[#766A62]', 'Pattern Cutting': 'border-[#C7DDD3] bg-[#EDF5F0] text-[#4E7357]', 'First Fitting': 'border-[#ECD8A7] bg-[#FFF7E3] text-[#8A6618]', 'Final Alterations': 'border-[#E6C8C2] bg-[#FDF0ED] text-[#9E5B4B]', 'Ready for Pickup': 'border-[#B9DDD0] bg-[#E7F4EE] text-[#277257]' };
const STAGE_ORDER: OrderStatus[] = ['Measuring', 'Pattern Cutting', 'First Fitting', 'Final Alterations', 'Ready for Pickup'];
const STAGE_CHART_COLORS: Record<OrderStatus, string> = { Measuring: '#C9BBA6', 'Pattern Cutting': '#8FAF9E', 'First Fitting': '#C9A15C', 'Final Alterations': '#A8644A', 'Ready for Pickup': '#6E8F72' };

function ChartTooltip({ active, payload, label }: any) {
  if (!active || !payload?.length) return null;
  return (
    <div className="rounded-lg border border-[#E2D7C7] bg-[#FFFCF8] px-3 py-2 shadow-lg">
      <div className="text-[10px] uppercase tracking-[0.14em] text-[#8C7E74]" style={{ fontFamily: "'Space Mono', monospace" }}>{label}</div>
      <div className="text-[13px] font-semibold text-[#2A211D] mt-0.5">{payload[0].value} job card{payload[0].value === 1 ? '' : 's'}</div>
    </div>
  );
}

export function FrontDeskOrdersView() {
  const [orders, setOrders] = useState(INITIAL_ORDERS); const [query, setQuery] = useState(''); const [createOpen, setCreateOpen] = useState(false); const [selected, setSelected] = useState<Order | null>(null); const [notice, setNotice] = useState('');
  const filtered = useMemo(() => orders.filter((order) => `${order.id} ${order.customer} ${order.garment} ${order.status}`.toLowerCase().includes(query.toLowerCase())), [orders, query]);
  const stageChart = useMemo(() => STAGE_ORDER.map((stage) => ({ stage, count: orders.filter((o) => o.status === stage).length })), [orders]);
  const balanceDue = useMemo(() => orders.filter((o) => o.payment === 'Balance due').length, [orders]);
  const createOrder = (form: NewOrderForm) => { const id = `JC-${Math.floor(3000 + Math.random() * 900)}`; const due = form.dueDate ? new Date(`${form.dueDate}T00:00:00`).toLocaleDateString(undefined, { month: 'short', day: '2-digit' }) : 'Not set'; setOrders((current) => [{ id, customer: form.customer, garment: form.garment, fabric: form.fabric || 'Not set', due, status: 'Measuring', payment: 'No payment yet' }, ...current]); setCreateOpen(false); setNotice(`${id} was opened for ${form.customer}.${form.scheduleFitting ? ` First fitting: ${new Date(`${form.fittingDate}T${form.fittingTime}`).toLocaleString(undefined, { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' })}.` : ''}`); window.setTimeout(() => setNotice(''), 5000); };
  return <div className="space-y-7"><div className="dash-in flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Label>Custom orders</Label><h1 className="mt-1 text-2xl sm:text-3xl text-[#2A211D]" style={{ fontFamily: "'DM Serif Display', serif" }}>Orders</h1><p className="mt-2 text-sm text-[#766A62]">Create job cards and monitor payment, due dates, and production stages.</p></div><button onClick={() => setCreateOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#2A211D] px-4 py-3 text-[10px] font-semibold uppercase tracking-[0.14em] text-white shadow-[0_10px_26px_-14px_rgba(42,33,29,0.55)] hover:bg-[#47382F]"><FilePlus2 className="h-4 w-4" /> Create order</button></div>{notice && <div className="dash-in rounded-lg border border-[#8B9E87]/40 bg-[#F1F5F0] px-4 py-3 text-sm text-[#4E7357] shadow-sm">{notice}</div>}<div className="dash-in grid grid-cols-2 gap-4 lg:grid-cols-4"><Metric label="Open job cards" value={orders.length} /><Metric label="Ready for pickup" value={orders.filter((o) => o.status === 'Ready for Pickup').length} tone="good" /><Metric label="Balances due" value={balanceDue} tone={balanceDue ? 'warn' : 'default'} /><Metric label="In production" value={orders.filter((o) => o.status !== 'Ready for Pickup').length} /></div><section className="dash-in dash-card rounded-xl p-6 sm:p-7"><div className="flex items-start justify-between gap-3 mb-1"><div><Label>Workshop floor</Label><h2 className="text-xl font-normal mt-0.5 text-[#2A211D]" style={{ fontFamily: "'DM Serif Display', serif" }}>Production pipeline</h2></div><span className="inline-flex items-center gap-1 rounded-full bg-[#F1F5F0] border border-[#C7DDD3] px-2.5 py-1 text-[10px] font-semibold tracking-[0.08em] uppercase text-[#4E7357] flex-shrink-0"><TrendingUp className="w-3 h-3" /> {orders.length} active</span></div><p className="text-[12.5px] text-[#8C7E74] mb-5">Job cards grouped by their current production stage</p><div className="h-44 -ml-2"><ResponsiveContainer width="100%" height="100%"><BarChart data={stageChart} margin={{ top: 8, right: 8, left: 0, bottom: 0 }}><CartesianGrid vertical={false} stroke="#ECE3D8" strokeDasharray="3 4" /><XAxis dataKey="stage" tickLine={false} axisLine={false} tick={{ fill: '#A3958B', fontSize: 10, fontFamily: 'Space Mono, monospace' }} interval={0} /><YAxis hide allowDecimals={false} /><Tooltip content={<ChartTooltip />} cursor={{ fill: '#F8F3EB' }} /><Bar dataKey="count" radius={[6, 6, 0, 0]} maxBarSize={44}>{stageChart.map((entry) => <Cell key={entry.stage} fill={STAGE_CHART_COLORS[entry.stage]} />)}</Bar></BarChart></ResponsiveContainer></div></section><section className="dash-card overflow-hidden rounded-xl"><div className="flex flex-col gap-4 border-b border-[#E8DFD3] p-5 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#A3958B]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search job card, customer, garment, or stage" className="w-full rounded-lg border border-[#E2D7C7] bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-[#A46B48]" /></div><span className="text-xs text-[#8C7E74]">{filtered.length} order{filtered.length === 1 ? '' : 's'}</span></div><div className="hidden grid-cols-[0.75fr_1.1fr_1.1fr_1.15fr_0.85fr_0.75fr_24px] gap-4 border-b border-[#E8DFD3] bg-[#FCFAF7] px-6 py-3 md:grid">{['Job card', 'Customer', 'Garment', 'Production', 'Payment', 'Due', ''].map((label) => <Label key={label}>{label}</Label>)}</div>{filtered.map((order) => <button key={order.id} onClick={() => setSelected(order)} className="grid w-full grid-cols-1 gap-2 border-b border-[#F0EAE2] px-6 py-4 text-left hover:bg-[#FCFAF7] md:grid-cols-[0.75fr_1.1fr_1.1fr_1.15fr_0.85fr_0.75fr_24px] md:items-center md:gap-4"><span className="text-[12px] text-[#8C6F3E]" style={{ fontFamily: "'Space Mono', monospace" }}>{order.id}</span><span className="font-medium text-[#2A211D]">{order.customer}</span><span className="text-sm text-[#5E5048]">{order.garment}</span><span><span className={`inline-block rounded-md border px-2 py-1 text-[10px] uppercase tracking-[0.08em] ${stageStyle[order.status]}`}>{order.status}</span></span><span className={`text-sm ${order.payment === 'Balance due' ? 'text-[#9E5B4B]' : 'text-[#5E5048]'}`}>{order.payment}</span><span className="text-sm text-[#5E5048]">{order.due}</span><ChevronRight className="hidden h-4 w-4 text-[#A46B48] md:block" /></button>)}{!filtered.length && <p className="p-12 text-center text-sm text-[#766A62]">No order matches your search.</p>}</section>{createOpen && <CreateOrderModal onClose={() => setCreateOpen(false)} onCreate={createOrder} />}{selected && <OrderDetails order={selected} onClose={() => setSelected(null)} />}</div>;
}
function Label({ children }: { children: React.ReactNode }) { return <span className="text-[10px] uppercase tracking-[0.2em] text-[#8C7E74]" style={{ fontFamily: "'Space Mono', monospace" }}>{children}</span>; }
function Metric({ label, value, tone = 'default' }: { label: string; value: number; tone?: 'default' | 'good' | 'warn' }) { const toneClasses = tone === 'good' ? 'text-[#4E7357]' : tone === 'warn' ? 'text-[#9E5B4B]' : 'text-[#2A211D]'; return <div className="dash-card rounded-xl p-5"><div className={`text-2xl ${toneClasses}`} style={{ fontFamily: "'DM Serif Display', serif" }}>{value}</div><Label>{label}</Label></div>; }
function OrderDetails({ order, onClose }: { order: Order; onClose: () => void }) { return <div className="fixed inset-0 z-50 flex items-center justify-center p-4"><button onClick={onClose} className="absolute inset-0 bg-[#1F1916]/40" /><section className="relative w-full max-w-xl rounded-xl bg-white p-7 shadow-2xl"><button onClick={onClose} className="absolute right-5 top-5 text-[#766A62]"><X className="h-5 w-5" /></button><Label>Job card details</Label><h2 className="mt-1 text-3xl text-[#2A211D]" style={{ fontFamily: "'DM Serif Display', serif" }}>{order.id}</h2><p className="mt-1 text-sm text-[#766A62]">{order.customer} · {order.garment}</p><div className="mt-6 grid grid-cols-2 gap-3">{[['Fabric', order.fabric], ['Due date', order.due], ['Production stage', order.status], ['Payment', order.payment]].map(([label, value]) => <div key={label} className="rounded-lg border border-[#E2D7C7] p-3"><Label>{label}</Label><div className="mt-1 text-sm text-[#2A211D]">{value}</div></div>)}</div></section></div>; }
export default FrontDeskOrdersView;
